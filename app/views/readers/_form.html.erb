<%= form_with(model: reader) do |form| %>
  <% if reader.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(reader.errors.count, "error") %> prohibited this reader from being saved:</h2>

      <ul>
        <% reader.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :reader_id, style: "display: block" %>
    <%= form.number_field :reader_id %>
  </div>

  <div>
    <%= form.label :join_date, style: "display: block" %>
    <%= form.date_field :join_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
